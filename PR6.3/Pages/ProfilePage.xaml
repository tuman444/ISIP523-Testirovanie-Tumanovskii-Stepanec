<Page x:Class="PR6._3.Pages.ProfilePage"
      Loaded="Page_Loaded"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:PR6._3.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ProfilePage">

    <Grid Background="#6b6b6b">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Background="#3d3d3d" Padding="20" VerticalAlignment="Top">
            <StackPanel>
                <Border Width="100" Height="100" CornerRadius="50" Background="LightGray" Margin="0,0,0,20">
                    <TextBlock Text="👤" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Foreground="#c20081"/>
                </Border>
                <TextBlock Text="Имя пользователя:"  FontSize="18" Foreground="White" FontWeight="Bold"/>
                <TextBlock x:Name="TxtName"  FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>

                <TextBlock Text="Логин" Foreground="White" FontWeight="Bold" FontSize="18"/>
                <TextBlock x:Name="TxtLogin"  FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>

                <TextBlock Text="Пароль" Foreground="White" FontWeight="Bold" FontSize="18"/>
                <TextBlock x:Name="TxtPassword" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>

                <Button Content="Выйти из аккаунта" Background="#c20081" Foreground="White" FontSize="18" FontWeight="Bold" Padding="5" Click="BtnLogout_Click"/>
                <Button Content="На главную" Background="#c20081" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,10,0,0" Click="BtnMain_Click"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1" Margin="30,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Мои билеты" FontSize="22" FontWeight="Bold" Foreground="White" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <ListView x:Name="LViewTickets" Grid.Row="1" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0,0,10,10" Width="700">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Black" BorderThickness="2" CornerRadius="8" Margin="0,0,0,10" Padding="15" Background="#c20081" Width="500">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock Text="{Binding Sessions.Movies.Title}" FontSize="18" FontWeight="Bold" Foreground="White"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,5">
                                        <TextBlock Text="{Binding Sessions.DateTime, StringFormat='dd MMMM HH:mm'}" Foreground="White" FontWeight="Bold"/>
                                        <TextBlock Text=" | " Foreground="White"/>
                                        <TextBlock Text="{Binding Sessions.Halls.Name}" Foreground="White" FontWeight="Bold"/>
                                    </StackPanel>
                                </StackPanel>

                                <Border Grid.Column="1" Background="#80005f" CornerRadius="5" Padding="10">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock Text="РЯД / МЕСТО" FontSize="10" Foreground="White" FontWeight="Bold"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding Seats.RowNumber}" FontWeight="Bold" FontSize="20" Foreground="White"/>
                                            <TextBlock Text=" - " FontWeight="Bold" FontSize="20" Foreground="White"/>
                                            <TextBlock Text="{Binding Seats.SeatNumber}" FontWeight="Bold" FontSize="20" Foreground="White"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock x:Name="TxtNoTickets" Text="У вас пока нет купленных билетов." 
            Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" 
            Foreground="Black" FontSize="16" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Page>
